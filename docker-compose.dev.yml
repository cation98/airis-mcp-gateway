services:
  mcp-gateway:
    ports:
      - "${GATEWAY_LISTEN_PORT:-9090}:${GATEWAY_LISTEN_PORT:-9090}"

  mcp-gateway-stream:
    ports:
      - "${GATEWAY_STREAM_LISTEN_PORT:-9091}:${GATEWAY_STREAM_LISTEN_PORT:-9091}"

  api:
    ports:
      - "${API_LISTEN_PORT:-9000}:${API_LISTEN_PORT:-9000}"

  settings-ui:
    ports:
      - "${UI_LISTEN_PORT:-5173}:${UI_CONTAINER_PORT:-5173}"
