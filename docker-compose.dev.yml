services:
  mcp-gateway:
    ports:
      - "${GATEWAY_LISTEN_PORT:-9390}:${GATEWAY_LISTEN_PORT:-9390}"

  mcp-gateway-stream:
    ports:
      - "${GATEWAY_STREAM_LISTEN_PORT:-9330}:${GATEWAY_STREAM_LISTEN_PORT:-9330}"

  api:
    ports:
      - "${API_LISTEN_PORT:-9900}:${API_LISTEN_PORT:-9900}"

  settings-ui:
    ports:
      - "${UI_LISTEN_PORT:-5173}:${UI_CONTAINER_PORT:-5173}"
