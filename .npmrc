# pnpm configuration
# Use hoisted node_modules for better compatibility with some tools
node-linker=hoisted

# Suppress build script warnings
# These packages will download binaries on first use if needed
# Set to false to see warnings, true to suppress them
ignore-scripts=false

# If you need to enable build scripts for specific packages:
# 1. Run: pnpm config set enable-pre-post-scripts true
# 2. Or create .pnpm-approvals.json with approved packages
